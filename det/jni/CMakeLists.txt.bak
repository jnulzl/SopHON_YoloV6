set(include_all ${include_all}
        ${CMAKE_CURRENT_SOURCE_DIR}/../
        ${PROJECT_SOURCE_DIR}/common
        )

file(GLOB _tmp ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
set(src_all ${src_all} ${_tmp})

set(JNI_LIB_NAME jnicls)

add_library(${JNI_LIB_NAME} SHARED ${src_all})

target_include_directories(${JNI_LIB_NAME} PRIVATE
        ${include_all}
        )

target_link_options(${JNI_LIB_NAME} PRIVATE
        -Wl,--warn-unresolved-symbols
        )

target_link_libraries(${JNI_LIB_NAME} PUBLIC
        ${CLS_LIB_NAME}
        log
        jnigraphics
        )